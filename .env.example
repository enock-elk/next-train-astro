# Copy to .env for local `astro dev`. Never commit .env.
# CARTO Voyager raster key. GitHub Actions repository secrets feed Actions builds.
# Cloudflare Pages Git previews (*.pages.dev) need the same name as a Pages env var
# (Production and Preview). The map page inlines this via ContentLayout.
PUBLIC_CARTO_API_KEY=
